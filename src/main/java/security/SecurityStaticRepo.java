package security;

public class SecurityStaticRepo {

    public static String getPermisos(String usuario) {
        return null; // this.usuariosPermisos.get(usuario);
    }

    public static Boolean verifyCredentials(String usuario, String password){

        /*
        String passwordStored = this.usuariosPasswords.get(usuario);

        if (passwordStored == null)
            return false;

        return  (passwordStored == password);*/

        return null;
    }

}
